# Block 6 Ranked Backlog

## Mandatory Host Measurement Prerequisite

Before any measured host-wall claim or final cross-lane decision, capture two strict external GitHub Copilot observations against the separate Todo fixture:

1. cold profile `copilot-todo-cold-01` starting with missing JSON 1;
2. warm profile `copilot-todo-warm-02` reusing current JSON 1.

Each observation binds repository commit, request digest, host/version, model identity, cache state, search/read counts, source bytes, authoring attempts, validation retries, elapsed wall, exposed input/output tokens, credits, and JSON 1/JSON 2 digests. Compare those external host profiles and the controlled GraphPilot anchor as separate denominators; never force percentages across incompatible repositories or requests. After a retained host-interface wave, rerun a comparable strict external profile before claiming measured savings.

The supplied cold observation is causal evidence but not a complete strict baseline. Warm profile 02 is missing. This blocks measured host-wall claims, not independently evidenced GraphPilot provider/runtime waves.

## Ranked Waves

| Rank | Lane | Owner | Measured/characterized contribution | Proof and retention gate |
| ---: | --- | --- | --- | --- |
| 1 | B | Semantic-review prompt–projection–schema–validator contract | 37,787.5872 ms response-repair call; `finding_invalid @ $` | Reproduce exact invalid shape offline; narrow contract correction; retain only if first review response is valid without response repair and all quality/provenance/compatibility/browser guards pass. |
| 2 | B | Generation prompt–projection–schema–validator contract | 26,052.5176 ms generation-repair call; `bdd_relationship_endpoints_invalid @ $.edges[3]` | Diagnose exact fourth-edge endpoint shape separately; retain only if first generation response is valid without generation repair and all guards pass. |
| 3 | A | GraphPilot host-facing context contract | External cold: about 406,024.6594 ms outside one readiness call, five authoring attempts/failures, relationship closure omitted | Add compact JSON 1/JSON 2 summaries/templates and exact relationship-closure guidance; provider-free characterize fewer correction steps. Wall benefit remains pending strict host rerun. |
| 4 | B | Provider packet/cache contract | 38,710 input tokens, 20,546 output tokens, 193,171 request bytes, zero cached tokens | Defer until first-response validity removes repair traffic; then characterize immutable repetition/cache eligibility. |
| 5 | A | MCP host integration | 865.7643 ms controlled MCP; 902.1306 ms provider-free schedule MCP | Separate startup from transport and prove persistent-session/reference-adapter behavior without protocol or recovery change. |
| 6 | B | Measured local sub-boundary | 3,995.3401 ms controlled local | Profile non-nested local work; do not optimize arbitrary Python. Retain only a measured material owner. |
| 7 | B | Editor load/render | 543.8 ms controlled browser | Profile only after larger provider/host contributors; preserve exact identity and clean console/network. |

## Selected First Wave

`wave-b1-semantic-review-contract` is selected. It is the largest high-confidence avoidable provider repair in the comparable controlled denominator. The larger external host observation is not a comparable warm bound profile, so it informs Lane A but does not displace the measured controlled first wave. Generation and semantic review remain separate causal owners.

## Guard and Reporting Contract

Every live proof uses a wholly new identity with at most nine calls and never reruns Block 5. The provider-free guard set covers fail-closed readiness, nontrivial generation, semantic review, canonical validation, provenance, browser identity, recovery/no-rerun, and compatibility. Each retained or reverted wave reports owner/domain; baseline/candidate identities; complete wall; call/token/repair effect; host read/correction effect when applicable; MCP/local/browser effect; quality/security/compatibility; rollback; cumulative improvement; and claim state (`measured`, `provider-free characterized`, or `pending external rerun`).
