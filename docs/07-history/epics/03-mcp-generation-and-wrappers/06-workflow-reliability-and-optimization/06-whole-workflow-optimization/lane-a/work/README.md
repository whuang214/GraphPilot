# Lane A Work Packages

Empty — Lane A is paused and no package is in flight.

One file per substantial package, named `<nn>-<name>.md`, holding that package's
whole lifecycle in order: plan, plan audit, implementation, implementation audit,
verification, optional live proof, decision, and Outcome. Minor work stays as a row
in [`../plan.md`](../plan.md) instead. Procedural gates never get their own file.

Files here stay at a stable path once created; they are not moved after each
milestone.
